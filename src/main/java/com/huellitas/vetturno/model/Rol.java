package com.huellitas.vetturno.model;

/**
 * Roles de VetTurno, USER es para recepción(Paula) y ADMIN puede hacer lo mismo que
 * recepción y además registrar veterinarios(Doña Marta).
 */
public enum Rol {
    USER,
    ADMIN
}
